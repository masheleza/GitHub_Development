@model MvcMusicStore.ViewModels.StoreBrowserViewModel

@{
    ViewBag.Title = "Browse Albums";
}

<h2>Browsing Genre: @Model.Genre.Name</h2>

<ul>
    @foreach (var album in Model.Album)
    {
        <li>@album.Title</li>
    }
</ul>